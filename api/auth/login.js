import { query } from '../../lib/db.js'; import { verifyPassword } from '../../lib/password.js'; import { setAuthCookie } from '../../lib/auth.js';
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});try{const {email,password}=req.body||{};if(!/^\S+@\S+\.\S+$/.test(String(email||''))||String(password||'').length<1)return res.status(400).json({error:'Email and password are required'});const r=await query('SELECT id,email,password_hash FROM users WHERE lower(email)=lower($1)',[String(email).trim()]);if(!r.rows[0]||!(await verifyPassword(String(password),r.rows[0].password_hash)))return res.status(401).json({error:'Invalid credentials'});setAuthCookie(res,r.rows[0]);return res.json({user:{id:r.rows[0].id,email:r.rows[0].email}});}catch(e){console.error(e);return res.status(500).json({error:'Login failed'});}}
